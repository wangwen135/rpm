package com.wwh.rpm.client.subserver;

import com.wwh.rpm.client.config.pojo.ClientConfig;

/**
 * 子服务管理器
 * 
 * @author wwh
 *
 */
public class SubserverManager {
    private ClientConfig config;

    public SubserverManager(ClientConfig config) {
        this.config = config;
    }

    // 创建全部子服务

    // 启动

    // 停止

}
